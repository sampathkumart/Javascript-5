// numbers

// typeof -> number
const run = 1;

//multiplication and addition
const s = 45;
const g = 65;

// math.round()
const hi = Math.round(20.4);
const hai = Math.round(20.5);

// math.floor()
const floor = Math.floor(20.6);

// math.ceil()
const ceil = Math.ceil(20.1);

// math.random()

const random = Math.random();
