// Array.of()
Array.of("how old are you", 20);

//Object.entries, Object.keys, Object.values
const hello = {
  bat: 15,
  ball: 10,
  helmet: 5
};

console.log(Object.entries(hello));
console.log(Object.keys(hello));
console.log(Object.values(hello));
